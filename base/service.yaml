apiVersion: v1
kind: Service
metadata:
  name: demo-appxyz
  labels:
    app: demo-appxyz
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: demo-appxyz
